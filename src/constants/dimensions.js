const padding = 50
const docWidth = 613
const docHeight = 792

module.exports = {
    width: docWidth - padding * 2,
    height: docHeight - padding * 2,
    left: padding,
    right: docWidth - padding,
    top: padding,
    bottom: docHeight - padding
}